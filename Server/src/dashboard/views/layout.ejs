<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HT2Peer Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="bg-gray-100">
    <div class="min-h-screen flex">
        <!-- Sidebar -->
        <div class="bg-gray-800 w-64 flex-shrink-0">
            <div class="flex items-center justify-center h-16 bg-gray-900">
                <span class="text-white text-xl font-bold">HT2Peer Admin</span>
            </div>
            <nav class="mt-5">
                <a href="/dashboard" class="flex items-center mt-4 py-2 px-6 text-gray-300 hover:bg-gray-700 hover:text-white">
                    <span class="mx-4">Dashboard</span>
                </a>
                <a href="/users" class="flex items-center mt-4 py-2 px-6 text-gray-300 hover:bg-gray-700 hover:text-white">
                    <span class="mx-4">Users</span>
                </a>
                <a href="/gamemodes" class="flex items-center mt-4 py-2 px-6 text-gray-300 hover:bg-gray-700 hover:text-white">
                    <span class="mx-4">Game Modes</span>
                </a>
                <a href="/matches" class="flex items-center mt-4 py-2 px-6 text-gray-300 hover:bg-gray-700 hover:text-white">
                    <span class="mx-4">Match History</span>
                </a>
                <a href="/leaderboard" class="flex items-center mt-4 py-2 px-6 text-gray-300 hover:bg-gray-700 hover:text-white">
                    <span class="mx-4">Leaderboard</span>
                </a>
                <a href="/settings" class="flex items-center mt-4 py-2 px-6 text-gray-300 hover:bg-gray-700 hover:text-white">
                    <span class="mx-4">Settings</span>
                </a>
            </nav>
        </div>

        <!-- Main Content -->
        <div class="flex-1">
            <header class="bg-white shadow">
                <div class="flex justify-between items-center px-6 py-4">
                    <h1 class="text-2xl font-semibold text-gray-900"><%- title %></h1>
                    <div class="flex items-center">
                        <span class="text-gray-500 mr-4"><%= user.username %></span>
                        <a href="/logout" class="text-gray-500 hover:text-gray-700">Logout</a>
                    </div>
                </div>
            </header>

            <main class="p-6">
                <%- body %>
            </main>
        </div>
    </div>

    <script src="/js/main.js"></script>
    <%- script %>
</body>
</html>
